---
import ExperimentRow from './ExperimentRow.astro';

interface Experiment {
  title: string;
  slug: string;
}

interface Props {
  experiments: Experiment[];
}

const { experiments } = Astro.props;

const base = import.meta.env.BASE_URL; // e.g. "/labrats-site/"

const withBase = (path: string) => {
  const cleanBase = base.endsWith('/') ? base.slice(0, -1) : base;
  return `${cleanBase}${path}`.replace(/\/{2,}/g, '/');
};
---

<section class="experiment-log">
  <div class="section-inner">
    <div class="section-header">
      <h2>Experiments</h2>
      <a href={withBase('/blog')} class="see-all-btn">See All</a>
    </div>

    <ul class="experiment-list">
      {
        experiments.map((exp, i) => (
          <ExperimentRow title={exp.title} slug={exp.slug} index={i} />
        ))
      }
    </ul>
  </div>
</section>

<style>
  .experiment-log {
    background-color: var(--color-bg-section);
    padding: 4rem 2rem;
  }

  .section-inner {
    max-width: 1400px;
    margin: 0 auto;
  }

  .section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1.5rem;
    gap: 1rem;
  }

  h2 {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 2.25rem;
    color: #1a1a2e;
  }

  .see-all-btn {
    background-color: var(--color-navy);
    color: #fff;
    font-family: var(--font-serif);
    font-size: 0.9rem;
    padding: 0.55rem 1rem;
    flex-shrink: 0;
    border-radius: 6px;
    text-decoration: none;
    white-space: nowrap;
  }

  .see-all-btn:hover {
    opacity: 0.9;
  }

  .experiment-list {
    list-style: none;
    border-radius: 4px;
    overflow: hidden;
  }

  @media (max-width: 600px) {
    .section-header {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      gap: 0.5rem;
    }

    h2 {
      font-size: 1.75rem;
      margin: 0;
    }
  }
</style>
