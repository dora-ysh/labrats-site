---
interface Props {
  title: string;
  slug: string;
  index?: number;
}

const { title, slug, index = 0 } = Astro.props;
const isAlt = index % 2 === 1;

const base = import.meta.env.BASE_URL; // e.g. "/labrats-site/"

const withBase = (path: string) => {
  const cleanBase = base.endsWith('/') ? base.slice(0, -1) : base;
  return `${cleanBase}${path}`.replace(/\/{2,}/g, '/');
};
---

<li class:list={['experiment-row', { alt: isAlt }]}>
  <span class="row-title">{title}</span>
  <a href={withBase(`/blog/${slug}/`)} class="details-btn">Details</a>
</li>

<style>
  .experiment-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.5rem 2rem;
    background-color: var(--color-bg-light);
  }

  .experiment-row.alt {
    background-color: var(--color-row-alt);
  }

  .row-title {
    font-family: var(--font-sans);
    font-size: 1.1rem;
    color: #1a1a2e;
  }

  .details-btn {
    background-color: transparent;
    color: var(--color-navy);
    font-family: var(--font-serif);
    font-weight: 700;
    font-size: 0.95rem;
    text-decoration: underline;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .details-btn:hover {
    opacity: 0.7;
  }

  @media (max-width: 600px) {
    .experiment-row {
      padding: 1.25rem 1.25rem;
    }

    .row-title {
      font-size: 1rem;
    }
  }
</style>
