---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import ExperimentLogSection from '../components/ExperimentLogSection.astro';

// TEMPORARY!! once i make actual posts, replace
// eg:
//
// import { getCollection } from 'astro:content';
// const posts = (await getCollection('blog'))
//   .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
//   .slice(0, 4);
// const experiments = posts.map((post) => ({
//   title: post.data.title,
//   slug: post.slug,
// }));

const experiments = [
  { title: 'Experiment Name (eg. Elephant Toothpaste)', slug: 'elephant-toothpaste' },
  { title: 'Experiment Name (eg. Elephant Toothpaste)', slug: 'experiment-2' },
  { title: 'Experiment Name (eg. Elephant Toothpaste)', slug: 'experiment-3' },
  { title: 'Experiment Name (eg. Elephant Toothpaste)', slug: 'experiment-4' },
];
---

<BaseLayout title="LABRATS — Sparking a lasting passion for STEM">
  <Hero />
  <ExperimentLogSection experiments={experiments} />
</BaseLayout>
